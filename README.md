# KTS Beauty

Mobile-first prototype for a premium beauty assistant. The app walks users through onboarding, a skincare questionnaire, routine generation, and a lightweight app shell.

## Run Locally

**Prerequisites:** Node.js


1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
